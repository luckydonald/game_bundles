In the repo /home/user/git/luckydonald/game_collections, I need context for a planned change: "HumbleBundle shall still be authoritative with its games, so not on humble = remove from bundle."

1. Read `merge_game_list()` in src/game_collections/sources/common.py in full — it currently NEVER removes an existing game absent from the fresh crawl (additive-only merge). Get its exact current code.
2. Read `_write_merged_game_list()` and both call sites of merge_game_list/atomic_write in src/game_collections/sources/humblebundle/crawler.py's `write_humble_offer()` function — get full context of that function.
3. Check src/game_collections/sources/isthereanydeal/crawler.py's `write_itad_offer()` (or equivalent write function) — does it also write into `lists/humblebundle/...` paths for Humble-sourced bundles, or does it write into `lists/isthereanydeal/...` exclusively? The CLAUDE.md says isthereanydeal "writes into every provider's own lists/<provider>/... directory, skipping bundles an existing dedicated scraper already covers" — find the actual code that implements this skip logic, and confirm ITAD never writes to lists/humblebundle/.
4. Look at `src/game_collections/models.py` for the `Game` model (fields, any "manually added" marker/flag distinguishing a manually-curated game from a scraped one) — is there ANY way to tell if a game in an existing list was manually added by a human vs scraped, that a removal-on-re-crawl policy would need to avoid deleting?
5. Check git log / lists/README.md for any prior discussion of manual edits to humblebundle lists being valuable (i.e., is it plausible a human manually adds an extra game entry to a lists/humblebundle/*.yml file for curation reasons, which "humble is authoritative -> remove if not on humble" would then destroy?).

Report under 400 words, code-referenced (file:line), factual: does merge_game_list's current additive-only design conflict with an intended "humble is authoritative, remove absent games" policy that would apply ONLY to humblebundle source, not other sources? Is there a way to distinguish manually-added games from scraped ones in the Game model today?