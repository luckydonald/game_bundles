Repo: /home/user/git/luckydonald/game_collections (Python 3.14+, Typer CLI named `game-collections`, entry point `game_collections.cli:app` in pyproject.toml).

I'm planning to add two new Typer subcommands to `src/game_collections/cli.py`: `install` and `uninstall`/`deinstall`. `install` will: detect the git repo root (via `git rev-parse --show-toplevel`), read the local package version from `pyproject.toml`, query PyPI's JSON API for the latest published version of the package name, semver-compare the two, and run either `uv tool install --editable <repo-root>` (git newer or PyPI has no release yet) or `uv tool install <name>` (PyPI newer) as a subprocess. It will also generate a bash+zsh completion script (supporting both bare `game-collections <TAB>` and `uv run game-collections <TAB>` completion) to a stable path, and auto-append an idempotent `source` line to the detected shell rc file (bash: ~/.bashrc or an rc.d-style directory if the user's setup uses one; zsh: ~/.zshrc). `uninstall`/`deinstall` will remove the completion script and the rc-file line it added, and separately ask (interactively, e.g. via typer.confirm) whether to also run `uv tool uninstall game-collections`.

Please report back (concisely, with file:line references):
1. `src/game_collections/cli.py`: overall Typer app structure, how existing commands are defined (decorators, imports, options style e.g. `typer.Option`), how they print output (plain print? `rich`? a shared console helper?), how they run subprocesses if any existing command does (e.g. does `scrape --git` or anything else shell out to `git`?), and error-handling conventions (raise `typer.Exit`? custom exceptions?).
2. `pyproject.toml`: project name, current `version`, full list of dependencies (do we already have `packaging`, `requests`/`httpx`, `rich`, `tomli`/`tomllib` usage?), and Python version floor (for `tomllib` stdlib availability).
3. `tests/test_cli.py`: how CLI commands are tested (Typer's CliRunner? subprocess? fixtures for temp dirs/HOME?) — enough detail to know how I'd write tests for `install`/`uninstall` that don't touch the real system (real `uv tool install`, real `~/.bashrc`).
4. Whether there's any existing subprocess-running helper/util in `src/game_collections/` (e.g. a wrapper around `subprocess.run` with consistent error handling) that should be reused, and whether there's an existing pattern for writing files to a user config/data directory (e.g. `~/.config/game-collections` or platformdirs usage) versus everything currently being relative to the repo's `config/`/`lists/` directories.
5. Any existing `--git` flag handling in `scrape humblebundle`/`scrape isthereanydeal` (mentioned in CLAUDE.md) — read that code, since it already does autostash/git-commit orchestration and might have reusable git-invocation patterns.

Report concisely, this is for planning purposes only — do not write any code.