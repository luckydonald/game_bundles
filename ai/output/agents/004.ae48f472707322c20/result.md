Clean working tree. Directive complete.

Implemented `game-collections scrape isthereanydeal`: crawls isthereanydeal.com's bundle aggregator (anonymous bootstrap token + paginated discovery API, verbatim-validated via a strict `ItadListSummary` model with the ITAD-internal tier/game preview deliberately excluded), extracts real cumulative tiers and per-game storefront IDs positionally from each bundle's own detail page (covers named tiers, flat-price bundles, and "Build Your Own" bundles uniformly), and writes into **every** provider's own `lists/<provider>/` tree — new `fanatical/`/`indiegala/`/`allyouplay/` included — while deduping against bundles already covered by the dedicated Humble/GreenManGaming scrapers (including a special case for Humble Choice's differently-named `choice/<YYYY-MM>.yml` files). Mature-rated bundles are skipped (no scrapeable game list behind their age gate).

Also did the planned prep refactor first: extracted `StoreName`/`STORE_ROOTS`/`parse_store_identity`/`normalized_title` out of `humblebundle/resolver.py` into a shared `sources/storefronts.py` (commit `34cb831`), since GreenManGaming's resolver and the new isthereanydeal source both need that URL-parsing logic without Humble's title-search machinery.

Test suite: 131 passing (up from 106). Live smoke test against the real site: 2 mature bundles correctly skipped, 9 already-covered Humble/GMG bundles correctly deduped, remaining Fanatical/IndieGala bundles produced real `lists/fanatical/`/`lists/indiegala/` entries with resolved `steam:<appid>` ids, and a second run confirmed full cache-resume. Committed as `c152f25`.