Implement the approved plan at /home/user/.claude/plans/steady-swinging-umbrella.md in full: a new `game-collections scrape isthereanydeal` source. Read that plan file now — it has the complete design (API mechanics, models, parser, crawler, dedup/naming rules, CLI, schema, tests, docs). This is a full feature implementation task (write code, tests, docs, schema, and commit at the end) — not research; the research is already done and is in the plan file plus the fixtures below.

## Fresh fixtures already fetched for you (use these, don't re-fetch unless you need more examples)
Directory: `/tmp/claude-1000/-home-user-git-luckydonald-game-collections/cd0d5bf6-9b5e-4b83-9236-5f2b9cc6dccb/scratchpad/itad/`
- `bundles_page.html` — the anonymous bootstrap page (`GET /bundles/`). Contains the inline `"token":"..."` and `"shops":{"<id>":["<name>",<flag>],...}` blobs described in the plan.
- `cookies.txt` — the cookie jar from that same GET (Netscape format, has `sess2`).
- `token.txt` — the extracted token value (same string as the `sess2` cookie value) for quick reference.
- `list_live_offset0.json`, `list_expired_offset0.json`, `list_pending_offset0.json` — real `POST /bundles/api/list/?tab=<tab>` responses at offset 0, for all three tabs, using the token above. Use these as the basis for parser test fixtures (trim to a handful of entries to keep fixtures small, but keep at least one entry per interesting shape: a GreenManGaming one like id 16316, a Fanatical one like id 16375, a Humble Bundle one like id 16381, an IndieGala one like id 16339).
- `bundle_16316.html`, `bundle_16375.html`, `bundle_16381.html`, `bundle_16339.html` — real detail-page HTML for one bundle per provider (GreenManGaming/Fanatical/Humble Bundle/IndieGala respectively). Confirmed (during planning) that 16316 has `store.steampowered.com/app/<id>` links for all 6 games — check the others for what storefront links they actually contain (some may differ; the plan expects you to verify GOG/Epic/Ubisoft/Humble Store links too, not just Steam) and adapt the parser/tests accordingly to real observed structure, don't assume 16316's shape is universal.

Since these are already on disk with real API/HTML content, read them directly (Read tool or `grep`/`python3 -c "import json..."`) rather than re-curling — you have working cookies+token in case you do need one more live fetch (e.g. a 5th provider, or to confirm pagination past offset 30), but the token expires as an anonymous session so if a fetch fails, redo the bootstrap GET yourself (`curl -c cookies.txt https://isthereanydeal.com/bundles/` then regex the `"token":"..."` value out of the HTML) rather than assuming these files still work forever.

## Key things already learned that the plan file also has, called out here because they're easy to miss
- The `itad-sessiontoken` header value must exactly equal the `sess2` cookie value from the same anonymous bootstrap GET; requests also need `Accept: application/json` (default browser `Accept: */*` gets routed to the HTML fallback page, not the JSON API) and the `sess2` cookie must actually be sent. This is a plain anonymous CSRF-style token issued to any visitor — treat it as ordinary scraping bootstrapping, nothing user/credential-related.
- `page.name` in each list-API summary is the aggregator's label for the origin bundle-selling platform (seen: "Humble Bundle", "Fanatical", "GreenManGaming", "IndieGala", "AllYouPlay") — this is what step 0 of the plan's provider-mapping config keys off, and it directly matches this repo's existing `humblebundle`/`greenmangaming` provider slugs for those two.
- The bundle's real slug (matching what a dedicated scraper for that provider would use, e.g. our own `greenmangaming` source's `lists/greenmangaming/bundle/metroidvania-madness/`) comes from decoding the `u=` query parameter inside the summary's affiliate-redirect `url` field, then taking the last non-empty path segment.
- Do step 0 (extracting `StoreName`/`STORE_ROOTS`/`parse_store_identity`/`normalized_title` out of `src/game_collections/sources/humblebundle/resolver.py` into a new shared module, and repointing `humblebundle/resolver.py` + `greenmangaming/resolver.py` imports at it) as a clean first commit before writing any isthereanydeal-specific code — verify with `pytest tests/test_humblebundle_resolver.py tests/test_greenmangaming_resolver.py -q` that this refactor is behavior-preserving before moving on.
- Existing dedup targets to verify against during the manual smoke test: `lists/greenmangaming/bundle/lego-at-the-movies/` and `lists/greenmangaming/bundle/metroidvania-madness/` already exist on disk (built in an earlier session) — the isthereanydeal crawl finding bundles 16363 ("LEGO At The Movies") and 16316 ("Metroidvania Madness") should detect these and skip writing `lists/` for them (still write the `archives/isthereanydeal/...` record though).
- This repo's active `commit-with-lplp-style` skill governs commits — invoke it explicitly and follow it (message via `ai/git/pending-commit.md`, `-F` commit, explicit-path staging only, fold any stray session auto-commits per its rules). Check `git log --oneline -10` / `git status` first for any auto-commits sitting on top of the last real commit (`34b119a`, the GreenManGaming feature) that need folding in.

## Verification (per the plan's Verification section)
- `env UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q` fully green (not just new test files).
- `env UV_CACHE_DIR=/tmp/uv-cache uv run game-collections schema` regenerates cleanly, no drift on a second run.
- Real manual smoke test: `env UV_CACHE_DIR=/tmp/uv-cache uv run game-collections scrape isthereanydeal` against the live site. Confirm: the two GreenManGaming bundles above get skipped (logged) rather than rewritten; at least one Fanatical/IndieGala/AllYouPlay bundle produces new `lists/<provider>/...` files with real resolved storefront ids; `archives/isthereanydeal/bundle/<id>/source.json` contains the schema-validated (not raw-passthrough) list-summary data. Re-run once more and confirm cache-resume behavior (no needless re-fetching of already-archived bundles).

Do not touch the pre-existing untracked `archives/dailyindiegame/...`/`lists/dailyindiegame/...` files — unrelated in-progress work, not yours.

When done, report back: what you built, the step-0 refactor's test outcome, full test suite pass/fail counts, the live smoke-test outcome (which bundles got skipped vs written, which providers/storefronts you actually saw resolved ids for), and the final commit hash(es). Note anything that surprised you or deviated from the plan (e.g. if a provider's detail page turned out to have no direct storefront links at all, or the date-prefix/cadence logic needed adjustment). Keep the report factual and reasonably brief.