In the repo /home/user/git/luckydonald/game_collections, I'm investigating a suspected bug: the todo says "the order of tier-1 to tier-3 is reversed between humblebundle and isthereanydeal, which causes full file conflicts. The full bundle is the highest tier. I believe humblebundle has it wrong."

A prior research pass found:
- `src/game_collections/sources/humblebundle/parser.py` lines ~376-406 build a `tiers` list by iterating Humble's own `tier_order` array from the source page, and a test fixture there (`tests/test_humblebundle_parser.py` ~line 87/134) shows `tier_order: ["all", "basic"]` produces `["Entire 2 Item Bundle", "1 Item Bundle"]` — i.e. INDEX 0 = the full/entire tier, appearing FIRST in the list, before the smaller tier.
- `src/game_collections/sources/humblebundle/crawler.py` lines ~294-346 then does `rank = enumerate(archive.tiers or choice_pick_options, start=1)` and writes `tier-{rank}.yml` with `tier: rank` — i.e. it just numbers tiers in whatever order they already appear in `archive.tiers`, with NO re-sorting step by item count.
- Yet real on-disk data (`lists/humblebundle/bundle/2026-06-17_upload-vr-summer-2026/`) shows tier-1="2 Item Bundle" (smallest), tier-2="5 Item Bundle", tier-3="Entire 9 Item Bundle" (full/highest) — ascending order, full bundle LAST/highest-numbered.
- isthereanydeal's crawler (`src/game_collections/sources/isthereanydeal/crawler.py` ~459-521) does the same enumerate-in-existing-order pattern, but its parser preserves ITAD's own cumulative tier order which is inherently ascending (full last).

This is contradictory: if parser.py puts the full tier FIRST (index 0) and crawler.py does no re-sorting, real Humble Choice output should have tier-1=full, tier-2=smaller — but the actual on-disk example shows the opposite (ascending, full=tier-3). Please resolve this contradiction definitively:

1. Read `src/game_collections/sources/humblebundle/parser.py` in full around wherever `tiers`/`archive.tiers`/`choice_pick_options`/`tier_order` are built (search broadly, not just lines 376-406) — is there a sort-by-item-count step ANYWHERE between raw `tier_order` parsing and what `crawler.py` receives as `archive.tiers`? Quote the exact code.
2. Read `src/game_collections/sources/humblebundle/crawler.py` around tier-writing (~lines 260-370) in full — confirm exactly what iterable it enumerates over and in what order, for BOTH the Choice-picks path and the classic Games-bundle-tiers path (these may differ).
3. Check whether Humble's actual `tier_order` field on the real page for Choice months always lists the SMALLEST tier first (i.e. maybe the test fixture I was told about is atypical/wrong, or maybe I mis-read it) — check `tests/fixtures/` for real captured Humble Choice HTML/JSON if available, or any other test that sets up `tier_order` with more entries.
4. Do the same full trace for `src/game_collections/sources/isthereanydeal/parser.py` — where exactly does its tier order come from (raw source field name), and is it definitely ascending (smallest first) with no sorting needed, or does IT also rely on assuming source-order equals ascending order?
5. Search git log / blame for these tier-building sections for any recent bugfix or the word "revers" to see if this was already partially addressed.

Report under 450 words: the definitive resolution of the contradiction (which function, if any, sorts tiers, and by what key), whether humblebundle's current code is actually correct or buggy in general (not just for the one on-disk example, which might have been hand-fixed), and precisely which line(s) would need to change if it IS buggy. Code-referenced (file:line) throughout.