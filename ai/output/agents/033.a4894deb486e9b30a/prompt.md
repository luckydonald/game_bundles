In this repo (game_collections, Python), I need full understanding of the Humble Bundle scraping pipeline for a plan to fix a Steam resolution issue.

Context: `scrape humblebundle` sometimes hits "Resolve 'X' on steam: Multiple matches / Other" prompts when resolving a Humble bundle item's title to a Steam storefront ID. For the item "Steelrising - Bastille Edition" (from https://www.humblebundle.com/games/dread-and-dark-fantasies-rpg-collection), the resolver can't cleanly match because Steam's actual store page for "Steelrising" (app 1283400) sells it via a "Buy Steelrising - Bastille Edition" purchase option that bundles a base game + DLCs (steamdb.info/sub/729916 lists package apps: Steelrising, Steelrising - Cagliostro's Secrets, Steelrising - Discus Chain). This matches Humble's own bundle description which apparently lists these as sub-items already (recent commits deal with "dlc pack gating" — humble items whose description text is itself a DLC pack that should be split into multiple fake games, vs. false positives where a normal game's description was wrongly split).

Please investigate and report:
1. `src/game_collections/sources/humblebundle/resolver.py` — how title-to-Steam-appid resolution works, what "Multiple matches" / interactive prompt flow looks like, where it's invoked from crawler.py.
2. `src/game_collections/sources/humblebundle/parser.py` — the current "DLC pack gating" logic (recently added per commits 177ae837f, caf56756e, d5993ee9e) that detects when an item's description describes a DLC pack and splits it into multiple synthetic games/items. Show relevant functions/classes, how it decides an item IS a dlc pack vs a normal game, and how sub-item titles are extracted from description text.
3. `src/game_collections/sources/humblebundle/steamdb.py` — what it's used for (steamdb.info package/sub lookups?), and whether it already has any concept of Steam "sub"/package IDs vs appids.
4. `src/game_collections/sources/humblebundle/models.py` — relevant models (Item, DlcPack, resolved ids: source markers like `unresolved:source:...`, `steam:<appid>` format, etc.)
5. Existing tests for dlc pack gating (probably tests/test_humblebundle_parser.py or similar) — show a representative test case (input description text -> expected split) so I understand the exact test pattern/fixture style used, since the user wants "add a test for that" using this Steelrising bundle as a new fixture case.
6. Search for how `complete`/`sync` command handles an item resolving to multiple different Steam apps that are actually a package/bundle purchase (i.e., is there already any handling of "this Humble item maps to a Steam package containing N apps" as opposed to a single appid)? Search for "sub:" or "package" concepts in the codebase.
7. Any existing handling of Steam "Buy X - Y Edition" bundled purchase options / sub packages during resolution — search resolver.py and storefronts.py (src/game_collections/sources/storefronts.py) for how Steam search results / store page scraping works and whether package/sub IDs are already recognized anywhere as a resolvable identity distinct from appid.

Report file paths with line numbers, relevant code snippets, and a clear description of the current architecture and the exact gap that needs to be filled to support this new "DLC pack via description text matching Steelrising base+2 DLC" case. Keep the report thorough but organized — this is for a Plan agent, not for a human reading it live. Do not modify any files.