Repo: game_collections (Pydantic-validated YAML game lists + Steam launcher sync, Python 3.14+, Typer CLI). CLAUDE.md rules: complete type annotations, strict Pydantic models, early returns, `# end if`/`# end for`/etc. comments closing every indent level, never remove existing comments, treat unknown external fields as errors, update README/lists/README.md on public behavior changes, IDs are relative file paths (without .yml) and must never be touched by display-name changes.

I need an implementation plan covering 4 independent todo items. Design each separately (they can likely be done as separate small PRs/commits per project's commit-per-task style), but produce one combined plan document.

## Item 1: Standardize bundle tier name casing between humblebundle and isthereanydeal
- `src/game_collections/sources/humblebundle/parser.py` builds tier names directly as title-space-case: `f"Entire {item_count} Item Bundle"` / `f"{item_count} Item Bundle"` (inline in `parse_choice_month`, ~line 390).
- `src/game_collections/sources/isthereanydeal/parser.py` has `_humble_tier_name(item_count, expected_game_count)` (lines ~245-254) which builds the SAME concept but in dash-snake-case: `f"{prefix}{item_count}-item-bundle"` where prefix is `"entire-"` or `""`. This is only used when mimicking humblebundle-sourced ITAD bundles (provider_slug == "humblebundle").
- This name is purely a display string (`GameList.name` / YAML `name:` field) — NOT used for any path/ID/slug (those use a separate `real_slug`/`machine_name`/date-prefix scheme). So it's safe to change without touching IDs.
- No existing shared slug->title normalization helper exists in `src/game_collections/sources/common.py`.
- Existing test `tests/test_isthereanydeal_parser.py:321` currently asserts `tiers[0].name == "entire-1-item-bundle"` and will need updating to the title-space-case form to match humblebundle's convention (e.g. "Entire 1 Item Bundle").
- Design the exact fix: should `_humble_tier_name` just be rewritten to produce `f"Entire {item_count} Item Bundle"` / `f"{item_count} Item Bundle"` directly (duplicating humble's format string), or should a shared helper be factored out in common.py and used by both parsers? Recommend the simplest correct approach given CLAUDE.md's "launcher-neutral"/reuse guidance, and note any archive schema regeneration implications (CLAUDE.md says regenerate schemas after changing public list/archive models — tier NAME is likely just a string field already in the schema, so probably no schema change needed, but confirm this assumption is safe to state in the plan).

## Item 2: Persist apply TUI filter settings into config/apply-selection.yml
- `src/game_collections/apply/tui.py`: filter state lives only as instance attributes never written out: `_Filters` dataclass (min_items, max_items, date_after, date_before) in `self._row_filters`; `self.min_missing`/`self.max_missing`; `self.unresolved_handling`; `self.unconfigured_handling`; `self.tier_mode` ("highest"/"all"); `self._show_filtered` (bool).
- `_build_selection()` (~line 1109-1123) builds `ApplySelection(schema=1, selected=…, excluded=…, updated_at=…)`. Save happens via `action_save()` → `ApplyActionScreen` → actual write in `cli.py` line ~1168 calling `save_selection(selection, selection_config)`.
- `src/game_collections/apply/config.py` lines 26-34: current `ApplySelection` model fields: `schema_version` (alias "schema", Literal[1]), `selected: list[str]`, `excluded: list[str]`, `updated_at: datetime`. `load_selection` uses strict JSON-mode Pydantic validation (lines 37-58).
- Design: add new OPTIONAL fields to `ApplySelection` for the filter state (must default such that old saved YAML files without these fields still validate under strict mode — use `Field(default=...)` with sensible defaults, e.g. None for the min/max/date filters, "ignore" or whatever the current TUI default is for unresolved/unconfigured handling, "highest" for tier_mode, a bool default for show_filtered). Also design: where in tui.py these get READ BACK on startup to restore filter UI state when an apply-selection.yml already exists (so this isn't write-only) — find/confirm whether the picker currently loads an existing ApplySelection at startup at all (if so, that's the place to also restore filters). Recommend exact field names and Pydantic types, and where in `_build_selection()` to populate them from `self.*` filter state.

## Item 3: Rename "Unconfigured Handling"
- `--unconfigured-handling` is a Typer CLI option (`cli.py` lines ~922, 974, 1050) with help text "How games from stores without a URL builder count toward ownership."
- Threaded through `completion.py` (`unconfigured_handling` param ~line 34), `launchers/steam/adapter.py` (`SteamAdapterOptions.unconfigured_handling`, lines ~47, 60, 151), and TUI `self.unconfigured_handling` + `#filter-unconfigured-handling` `FilterSelect` widget (tui.py lines ~469, 533-544, 690, 867-877, 985-989), labeled "unconfigured: hide/ignore/enforce".
- Actual semantics (`completion.py::evaluate_completion`, lines 29-91): games are split into `unresolved` (no provider could be identified for any qualified ID at all) vs `unconfigured` (provider/store WAS identified, but that store has no ownership-verification support — no Steam app ID / no URL builder, e.g. non-Steam storefronts like GOG-only games). `unconfigured_handling` controls hide/ignore/enforce for the latter case specifically.
- Propose 2-3 candidate renames that make the distinction from "unresolved" clearer (e.g. `--unsupported-store-handling`, `--unownable-handling`, `--no-ownership-check-handling`), recommend one, and enumerate EVERY location needing a rename: CLI flag name + help text, Python param/attribute names across cli.py/completion.py/adapter.py/tui.py, TUI widget id `#filter-unconfigured-handling` and its label text, any saved config field name if item 2's plan adds `unconfigured_handling` to ApplySelection (coordinate naming between item 2 and item 3 — pick final name once, use everywhere including the new persisted field), and README/lists/README.md mentions of this flag (need to grep, not yet checked). Also flag whether this is a breaking CLI flag rename (yes) and whether a hidden backward-compat alias is warranted per CLAUDE.md's stance against compatibility shims (CLAUDE.md says avoid backwards-compat hacks — so plan should recommend a clean rename with no alias, documented as a breaking change).

## Item 4: humblebundle crawler should support merging with existing lists instead of overwriting
- Confirmed via investigation: NEITHER humblebundle NOR isthereanydeal crawler currently merges. `write_humble_offer()` (`humblebundle/crawler.py:247-352`) never reads the existing YAML at `path` before writing — builds a fresh `GameList` in memory purely from the crawled archive and calls `atomic_write(path, render_game_list_yaml(...))`, a full clobber. Same pattern in `isthereanydeal/crawler.py::write_itad_offer()` (422-523) for the lists it does write.
- `src/game_collections/sources/common.py` has `atomic_write`, `dump_json`, `load_cached_archive`, `render_game_list_yaml` — no read-before-write/merge helper exists.
- Risk: any manually-edited entries in an existing `lists/humblebundle/...yml` (e.g. manually added `ids:`, manually fixed unresolved markers, added `references`) get silently overwritten on the next scheduled crawl.
- Design a merge strategy: when `path` already exists, read+parse the existing `GameList` (via whatever loader `lists.py`/`models.py` provides — identify the exact function), and merge with the freshly crawled `GameList` such that: (a) games present in the old file but resolved differently/manually (e.g. a manual `ids:` entry that isn't `unresolved:...`) are preserved rather than clobbered by a freshly-resolved (possibly worse or same) value, (b) new games from the fresh crawl not in the old file are added, (c) games removed from the source (should this ever happen for a historical bundle?) — decide whether removal should ever happen or old entries should always be kept. Keep in mind CLAUDE.md: "Do not put launcher-specific behavior into YAML loading" doesn't apply here (this is source-side, not launcher), but "prefer enhancing, not overwriting" from the todo item itself is the guiding principle. Recommend where this merge logic should live (`common.py` as a shared helper both humblebundle and isthereanydeal writers can use, given item's todo also implies isthereanydeal should ideally follow the same pattern eventually, though the todo item text only explicitly asks for humblebundle) and its exact function signature.

For all 4 items, identify the critical files/functions to modify (already mostly given above — confirm exact names), note test files that will need new/updated tests (tests/test_humblebundle_parser.py, tests/test_isthereanydeal_parser.py, tests/test_apply_*.py if it exists — check), and whether schemas/*.json regeneration is needed per CLAUDE.md (`uv run game-collections schema`). Output a structured plan per item: approach, files touched, key risks, test additions needed.